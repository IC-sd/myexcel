# 当前进度与交接

> 更新日期：2026-09-16
> 当前状态：**阶段 1 已完成；阶段 2 公开发布基线进行中。**

历史实施过程、旧检查点和逐轮测试数字由 Git 保留，本文件只维护当前事实、未完成项和下一步。

## 1. 项目定位

本项目是面向 GitHub 的通用类 Excel 应用构建平台，不是某家公司的 ERP、OA、排产或打印项目。产品保持两个使用视角：

- **设计模式**：配置表格模板、字段绑定、基础关系和发布版本。
- **运行模式**：在已发布模板中填写、保存、查询和重新打开记录。

阶段 1 只证明“表格模板可以成为结构化数据应用”；企业权限、审批中心、审计运维和复杂集成不是 MVP 完成条件。

## 2. 当前交接包

| 项目 | 当前事实 |
|---|---|
| 分支 | `phase1/foundation`，公开远程的 `main` 与该分支当前均指向干净公开基线 |
| 阶段 | 阶段 1 已完成；阶段 2 的本地实现和浏览器验收已完成，等待远程 CI 和首个 release |
| 产品入口 | `README.md`、本文件和 `PROJECT_BLUEPRINT.md` |
| 数据边界 | 只使用可再生成的合成示例；不得接入或提交真实业务资料 |
| 本地环境 | Node.js 24；SQLite 元数据与本地 JSON 业务记录默认启用，MySQL 13307 仅为可选适配器测试环境 |
| 当前阻塞 | GitHub 默认分支仍需在仓库设置中改为 `main`，之后才能删除远程 `phase1/foundation` |
| 下一步 | 推送并观察 GitHub Actions；确认发布 `v0.2.0` 首个公开 release |

## 3. 阶段 1 最小完成范围

| 编号 | MVP 能力 | 当前结果 |
|---|---|---|
| M-01 | 工作簿创建或导入、编辑、批量粘贴、保存和重开 | 已完成 |
| M-02 | 设计模式与运行模式 | 已完成 |
| M-03 | 稳定字段、主表/明细和固定/纵向区域绑定 | 已完成 |
| M-04 | 基础字段类型、常用公式和 XLSX 兼容提示 | 已完成 |
| M-05 | 业务记录保存、列表、打开和原网格回填 | 已完成 |
| M-06 | 稳定 ID 关联选择、字段带出和基础汇总 | 已完成 |
| M-07 | 草稿、发布和固定发布版本 | 已完成 |
| M-08 | 合成示例、自动测试、生产构建和浏览器闭环 | 已完成 |

旧 N-01—N-08 是实施过程的拆分方式，不再作为产品范围清单。其成果已经归并为以上 8 项 MVP 能力。

## 4. 已实现但不属于阶段 1 门槛

以下能力当前代码中可以保留，默认在界面中作为可选增强折叠展示；后续只按维护价值决定继续完善或移除：

- 服务端关键字段复算和破坏性发布检查；
- 发布历史回滚；
- 模板、记录和字段级示例权限；
- 简单提交/通过/退回状态流及受控回写；
- 操作与数据审计；
- MySQL 记录适配、备份辅助和合成性能基准；
- 日期/状态筛选、自动刷新和详细历史。

这些能力不能扩大阶段 2、3 的交付门槛。完整 BPMN、组织架构、SSO、联合备份恢复、打印设计器、企业通知和行业模块不在现行路线中。

## 5. 验证摘要与明确限制

最近一次完整基线验证为：非 MySQL 基础测试 56 项通过、11 项按预期跳过；隔离 MySQL 全量 67 项通过；Vite 生产构建通过；宽屏、窄屏和三种演示角色完成真实浏览器闭环，控制台无警告和错误。

2026-09-16 的轻量化调整只改变范围说明和默认界面层级，没有删除稳定服务端能力。数据来源/性能、模板权限、操作记录和简单状态流均改为默认折叠的可选增强。基础测试 56 项通过、11 项 MySQL 专项按未配置环境跳过；Vite 316 模块生产构建通过；本地浏览器确认设计页层级、折叠状态和响应式布局正常。

明确限制：

- 只承诺 `.xlsx` 基础导入与兼容提示，不承诺旧 `.xls` 或全部 Excel 高级特性；
- 不承诺多人实时协作、复杂 BPMN、企业组织权限或真实业务现场适配；
- Univer 工作区包体仍较大；
- 本地 JSON 记录与 SQLite 元数据不是统一事务；MySQL 适配器同样不与 SQLite 元数据组成跨库事务。

## 6. 阶段 2 进度与下一步

| 项目 | 状态 | 结果或下一步 |
|---|---|---|
| 默认零依赖记录存储 | 已完成 | 新增持久化本地 JSON 适配器；主从、关系、带出、汇总、幂等和重启重开有自动测试；MySQL 保留可选 |
| 跨平台命令与快速开始 | 本地已验收，待 Linux CI | 根 README 使用 Node/npm 通用命令；Windows 干净数据目录已验证，Linux 交给 CI，macOS 暂无独立证据 |
| 许可证和社区文件 | 已完成 | MIT、第三方说明、贡献指南、安全策略、Issue/PR 模板已加入 |
| 最小 CI | 已完成配置，待远程运行 | GitHub Actions 使用 Node 24 执行 `npm ci`、测试和构建 |
| 首个 release | 已准备，未发布 | `CHANGELOG.md` 已形成 `v0.2.0` 说明；浏览器视觉与控制台检查通过，远程 CI 通过后创建 |

阶段 2 完成后项目即可公开体验；阶段 3 再补稳定 API、模板包和一种通用数据交换能力。阶段 4 只选做一个展示增强，不作为核心完成门槛。
