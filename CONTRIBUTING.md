# Contributing

感谢你改进这个项目。请优先提交范围清楚、可以复现和验证的问题或改动。

## 本地检查

```bash
cd myexcel-platform
npm ci
npm test
npm run build
```

- 使用 Node.js 24 或更高版本。
- 默认本地记录模式不需要 MySQL；修改 MySQL 适配器时再运行隔离 MySQL 专项测试。
- 测试和示例只能使用合成数据，不得提交账号、令牌、真实业务资料、数据库或本机绝对路径。
- 功能修改应同时更新相关测试和当前文档，避免引入第二套表格、模型或记录内核。

## Pull Request

请说明问题、实现边界、验证命令和未覆盖项。大型功能先提交 Issue 讨论，避免实现方向与项目的轻量路线冲突。
